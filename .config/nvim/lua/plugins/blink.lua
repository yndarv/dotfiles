return {
  "saghen/blink.cmp",
  opts = {
    keymap = { preset = "default" },
    completion = {
      accept = { auto_brackets = { enabled = false } },
      menu = {
        auto_show = false,
      },

      ghost_text = { enabled = false },
    },
  },
}
